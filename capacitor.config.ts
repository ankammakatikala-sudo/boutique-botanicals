import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.boutique.botanicals',
  appName: 'Boutique Botanicals',
  webDir: 'dist'
};

export default config;
